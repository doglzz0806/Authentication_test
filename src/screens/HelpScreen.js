import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const HelpScreen = () => {
    return <Text style={{ fontSize: 48 }}>Help Screen</Text>;
};

const styles = StyleSheet.create({});

export default HelpScreen;