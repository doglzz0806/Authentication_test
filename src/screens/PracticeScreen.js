import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const PracticeScreen = () => {
    return <Text style={{ fontSize: 48 }}>Practice Screen</Text>;
};

const styles = StyleSheet.create({});

export default PracticeScreen;