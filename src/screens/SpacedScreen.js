import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const SpacedScreen = () => {
    return <Text style={{ fontSize: 48 }}>Spaced Learning Screen</Text>;
};

const styles = StyleSheet.create({});

export default SpacedScreen;